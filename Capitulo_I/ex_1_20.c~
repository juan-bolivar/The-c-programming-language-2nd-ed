#include <stdio.h>

#define n_cols 4 



main(){
  int c; 
  int max;
  int i ;


  max=0;
  
  while (( c = getchar()) != EOF)
    if( c !='\t'){

      putchar(c);

    }

    else{

      for(i=0 ; i <= n_cols ; i++) {
	putchar(' ');
      } 

    }
    
  return 0;

}


